/** A table to transrate the permission notation to the role name. */
export type role = "none" | "freeBusyReader" | "reader" | "writer" | "owner";
export const roleList = ["none", "freeBusyReader", "reader", "writer", "owner"];
